#########################################
# ~/.config/hypr/hyprlock.conf
# Self-contained: palette + config (no import)
#########################################

# ---- BEGIN AUTO-GENERATED PALETTE (from central scheme) ----
$bg        = rgb(39,41,50)   # #272932
$fg        = rgb(238,233,231)   # #EEE9E7
$accent    = rgb(253,245,0)   # #FDF500
$red       = rgb(204,0,0)   # #CC0000
$bg_hex     = #272932
$fg_hex     = #EEE9E7
$accent_hex = #FDF500
$red_hex    = #CC0000
# ---- END AUTO-GENERATED PALETTE ----

#########################################
# ~/.config/hypr/hyprlock.conf
#########################################

# pull in your central palette
source = ~/.config/hypr/colors.conf

general {
    disable_loading_bar = true
    hide_cursor = true
    no_fade_in = false
}

background {
    # uses your current screen as blurred backdrop
    path = screenshot
    blur_passes = 3
    blur_size = 8
}

# --- Input field (password box) -----------------------------------
input-field {
    size = 200,50
    position = 0,0
    monitor =
    dots_center = true
    fade_on_empty = false

    # colors from central scheme
    font_color   = $fg
    inner_color  = $bg           # field fill
    outer_color  = $accent       # ring/border

    outline_thickness = 5
    placeholder_text  = Enter Password
    shadow_passes = 2
}

