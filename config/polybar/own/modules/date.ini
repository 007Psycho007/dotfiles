[module/date]
type = internal/date
interval = 1

time = "%H:%M"
time-alt = "%a, %b %d %H:%M:%S"

format = <label>
format-prefix = %{T6} %{T-}
format-prefix-background = ${colors.modules_date} 
format-prefix-foreground = ${colors.foreground-black}
format-prefix-padding-left = 1
format-underline = ${colors.modules_date}
label = %{T1}%time%%{T-}
label-background = ${colors.module}
label-foreground = ${colors.foreground}
label-padding = 1
